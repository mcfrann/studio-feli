import styles from "./style.module.scss";
import Logo from "../../SVGs/Logo/logo";

const Hero = () => {
  return (
    <section className={styles.hero}>
      <Logo />
    </section>
  );
};

export default Hero;
